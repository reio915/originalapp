<%= form_for @tweet do |f| %>
  <div class="field">
    <%= f.label :photo %>
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :comments %>
    <%= f.text_area :body, :size=>"50x10" %>
  </div> 
  <%= f.submit "編集する" %>
<% end %>